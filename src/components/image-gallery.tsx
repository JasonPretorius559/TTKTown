"use client";
import Image from "next/image"; import { useState } from "react";
export function ImageGallery({ images, alt }: { images:string[]; alt:string }){const available=images.filter(Boolean);const [selected,setSelected]=useState(0);if(!available.length)return null;return <div><div className="detail-image"><Image src={available[Math.min(selected,available.length-1)]} alt={alt} width={900} height={900}/></div>{available.length>1&&<div aria-label="Image gallery" style={{display:"flex",gap:10,marginTop:12,overflowX:"auto",overscrollBehaviorInline:"contain"}}>{available.map((src,index)=><button aria-label={`Show image ${index+1} of ${available.length}`} aria-current={index===selected?"true":undefined} type="button" key={src} onClick={()=>setSelected(index)} style={{border:index===selected?"3px solid var(--berry)":"1px solid var(--line)",borderRadius:12,padding:0,overflow:"hidden",background:"white",flex:"0 0 auto"}}><Image src={src} alt="" width={72} height={72} style={{width:72,height:72,objectFit:"cover"}}/></button>)}</div>}</div>}
